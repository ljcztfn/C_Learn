#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int i, j;
	float f, g;
//	int n = scanf("%d%f", &i, &f);
//	scanf("i = %d, f = %f\n", &i, &f);	//不建议使用
	scanf("%d/%d%f%f", &i, &j, &f, &g);
 	return 0;
}